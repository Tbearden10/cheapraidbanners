{
  "name": "cheapraidbanners",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Frontend is static' && exit 0",
    "dev": "wrangler dev --local --assets ./public",
    "deploy-worker": "wrangler deploy",
    "deploy-pages": "wrangler pages deploy ./public --project-name cheapraidbanners"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251125.0",
    "miniflare": "^4.0.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
