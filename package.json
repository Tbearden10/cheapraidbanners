{
  "name": "cheapraidbanners",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo \"noop\"",
    "dev": "wrangler dev --local --assets ./public",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "miniflare": "^4.0.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}