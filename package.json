{
  "name": "cheapraidbanners",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo Building frontend... && exit 0",
    "dev": "wrangler dev --local --assets ./public",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251125.0",
    "miniflare": "^4.0.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
